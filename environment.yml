name: diff_rendering
channels:
- nvidia
- pytorch
- iopath
- pytorch3d
- conda-forge  # Should be the last https://stackoverflow.com/a/69180871
dependencies:
- python>=3.9
- pip
- jupyterlab>=3.0
- jupyter
- ipykernel
- jupyter-lsp
- jupyterlab-lsp
- python-language-server
- jedi=0.17.2 # pinned because one of the language server packages needs it precisely pinned here
- torchvision
- pytorch-cuda
- pytorch3d
- tqdm
- matplotlib
- pytorch==2.0.0
- cudatoolkit==11.8
- pip:
  - opencv-python-headless
